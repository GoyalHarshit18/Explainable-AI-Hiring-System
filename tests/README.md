# Tests

This directory is reserved for unit and integration tests.

During development, validation was primarily performed through the Jupyter notebooks used to build and evaluate the pipeline. Future work will include automated tests for the parser, retrieval engine, feature engineering, scoring, and reasoning modules.